//Program in c to change the value of a variable by taking an input from the user
#include <stdio.h>
int main()
{
    int a=5;
    printf("Enter a number >>");
    scanf("%d",&a);
    printf("The new value of the variable >> %d",a);
}

